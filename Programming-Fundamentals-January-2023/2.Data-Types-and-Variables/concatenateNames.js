function concatenateNames (firstName, secondName, delimiter){

    let concatenation = firstName + delimiter + secondName;
    console.log(concatenation)
}

concatenateNames('Jan',
'White',
'<->'

)